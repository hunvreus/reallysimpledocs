import { Card, CardGrid, Code, Preview } from "reallysimpledocs/components";

export const gridSource = `<CardGrid>
  <Card title="Markdown" description="Write regular docs pages." href="/content/pages/" />
  <Card title="MDX" description="Use components for richer examples." href="/components/" />
</CardGrid>`;

export const cardSource = `<Card title="Custom width" description="Use class overrides when the layout needs it." class="max-w-sm" />`;

# Card and CardGrid

Use `Card` and `CardGrid` for small groups of related links, concepts, or next actions. Cards are intentionally simple: title, description, and an optional link.

## Usage

<Code
  lang="mdx"
  code={`import { Card, CardGrid } from "reallysimpledocs/components";

<CardGrid>
  <Card title="Markdown" description="Write regular docs pages." href="/content/pages/" />
  <Card title="MDX" description="Use components for richer examples." href="/components/" />
</CardGrid>`}
/>

## Options

| Component | Prop | Type | Default | Notes |
| --- | --- | --- | --- | --- |
| `Card` | `title` | `string` | `undefined` | Main card heading. |
| `Card` | `description` | `string` | `undefined` | Muted text below the title. |
| `Card` | `href` | `string` | `undefined` | Renders the card as a link. |
| `Card` | `class` | `string` | `""` | Extra classes applied to the card root. |
| `CardGrid` | `class` | `string` | `""` | Extra classes applied to the grid root. |

## Examples

<Preview code={gridSource} lang="mdx" class="block">
  <CardGrid>
    <Card title="Markdown" description="Write regular docs pages." href="/content/pages/" />
    <Card title="MDX" description="Use components for richer examples." href="/components/" />
  </CardGrid>
</Preview>

<Preview code={cardSource} lang="mdx" class="block items-start justify-start">
  <Card title="Custom width" description="Use class overrides when the layout needs it." class="max-w-sm" />
</Preview>
